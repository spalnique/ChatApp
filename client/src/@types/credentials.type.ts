export type LoginCredentials = {
  email: string;
  password: string;
};

export type RegisterCredentials = {
  displayName?: string;
  age: number;
  username: string;
  email: string;
  password: string;
};
